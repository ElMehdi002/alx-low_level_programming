#include "main.h"
/**
 * _isdigit - Check for a digit (0-9)
 * @c: Integer holding the character
 *
 * Return: 1- Digit 0- Not
 */
int _isdigit(int c)
{
	int i;

	if (i >= 1 && i <= 9)
		return (1);
	else
		return (0);
}
